# Myference Monad Testnet Evidence

No transaction evidence has been recorded yet. Run `scripts/e2e-testnet.sh` with a deployed Monad testnet contract, funded wallets, hosted broker, PostgreSQL, and a connected physical Windows Ollama provider. The script replaces this file only after it verifies every setup transaction, streams a real request, and observes its indexed settlement.
